name: Run Pingr ML Tracker

on:
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install pandas numpy

      - name: Run ML tracker
        run: |
          python pingr_ml_tracker.py

      - name: Upload ALL analysis files
        uses: actions/upload-artifact@v4
        with:
          name: pingr_analysis_outputs
          path: |
            pingr_cleaned_data.csv
            momentum_cycles.csv
            momentum_per_symbol.csv
            momentum_by_hour.csv
            momentum_rsi_buckets.csv
            momentum_heat_impact.csv
            momentum_macd_stats.csv
            momentum_advanced_summary.json
